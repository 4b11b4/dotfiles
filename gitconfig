[credential]
	helper = cache --timeout=21600
[user]
	email = antonbilbaeno@gmail.com
	name = anton
[push]
	default = simple
[alias]
	ca = !git add . && git commit -m
        suir = !git submodule update --init --recursive
        stat = !git status
        subup = !git pull --recurse-submodules && git submodule update
        adog = !git log --all --decorate --oneline --graph
